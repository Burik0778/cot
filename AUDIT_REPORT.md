# AUDIT_REPORT

Аудит проекта COT Research Platform, проход второй.

Всё ниже получено осмотром кода и прогоном, а не предположениями. Где
проверить было нельзя — так и написано.

Дата: 2026-08-31 · тестов на момент отчёта: 188 (`python scripts/count_tests.py`)

---

## Итог одной строкой

Из 99 разделов задания в этом проходе закрыт **P0 целиком** и часть
инфраструктуры под него. P1–P4 не тронуты. Проект НЕ готов по вашему
определению из раздела 98 — список незакрытого в конце, без смягчений.

---

## Исправлено в этом проходе

| # | Проблема | Severity | Файл | Почему это важно | Что сделано | Тест |
|---|---|---|---|---|---|---|
| 1 | `date(2999,1,1)` в тесте ронял pandas 2.x с `OutOfBoundsDatetime` | **Critical** | `tests/test_analog_engine.py` | Потолок `datetime64[ns]` — 2262-04-11. У автора pandas 3.x (`datetime64[s]`) тест проходил, у пользователя падал. Набор тестов не был воспроизводим между средами. | Дата заменена на 2030 | `TestDateHandlingRobustness` |
| 2 | **Та же конверсия в боевом коде защиты от look-ahead** | **Critical** | `src/analogs/similarity.py::_assert_pool_is_available` | Это главное: на pandas 2.x защита падала с `OutOfBoundsDatetime` вместо честного `LookaheadError`. То есть механизм, ради которого всё строилось, на половине сред не работал. Симптом был виден как «сломанный тест», причина лежала глубже. | Сравнение дат без `pd.to_datetime`, через `_to_date` | 4 регрессионных теста, включая дату за границей ns |
| 3 | Захардкоженное число тестов в README | Medium | `README.md` | «132 tests — all pass» разошлось с действительностью. Цифра, которую нельзя получить командой, врёт всегда. | `scripts/count_tests.py [--update-readme]`, маркеры в README | — |
| 4 | Нет способа сверить данные с CFTC | **Critical** | отсутствовал | Перцентили, аналоги, вся статистика стоят на исходных числах. Без сверки надстройка бессмысленна. | `scripts/validate_cftc.py` — построчная сверка Long/Short/OI, независимый пересчёт Net, проверка тождества, снимок в `data/validation/`, коды возврата 0/1/2 | прогнан, честно вернул BLOCKED |
| 5 | Набор не запускался через pytest | Medium | отсутствовали | Требование задания | `pytest.ini`, `conftest.py`; тесты на unittest подхватываются pytest как есть | не проверено, см. ниже |

---

## Найдено, НЕ исправлено

| # | Проблема | Severity | Файл | Почему это важно | Статус |
|---|---|---|---|---|---|
| 6 | `pd.to_datetime` ещё в трёх местах | Low | `data_quality.py:25,118`, `db.py:149`, `price_client.py:50,68` | Тот же класс уязвимости | Оставлено намеренно: там обрабатываются реальные даты CFTC и котировок, выход за 2262 год невозможен. Менять без нужды — риск без выгоды. Отмечено здесь, чтобы не забылось. |
| 7 | Перекрывающиеся наблюдения | **High** | `src/analogs/validity.py` | Недельные наблюдения с горизонтом 8–12 недель делят почти всю историю цены. Все доверительные интервалы и p-values оптимистичны. | Задокументировано в `STATISTICS.md`, но не исправлено. Нужны non-overlapping и block bootstrap (разделы 20–21). |
| 8 | Analog Engine смешивает COT и цену | **High** | `config/settings.py::DEFAULT_ANALOG_FEATURES` | `price_chg_8w_z` в наборе по умолчанию. Нельзя ответить на вопрос «есть ли преимущество в самом COT». | Не сделано. Нужны режимы COT ONLY / COT + PRICE (раздел 13). |
| 9 | Event study без отрицательных горизонтов | Medium | `src/events/event_study.py` | Предсобытийные окна возвращают NaN — колонок не существует | Не сделано (раздел 30). |
| 10 | Множественное тестирование | **High** | `src/analogs/validity.py` | Есть только предупреждение после 20 гипотез. Нет FDR, нет разделения Discovery/Confirmatory. | Не сделано (разделы 25–26, 64–65). |
| 11 | Backtest без спреда, проскальзывания, финансирования | Medium | `src/backtest/engine.py` | Задокументировано в `BACKTESTING.md`, но результаты остаются идеализированными | Не сделано (раздел 33). |
| 12 | Нет PDF-отчёта | Low | `src/reporting/report.py` | Markdown и HTML есть, PDF нет | Не сделано (раздел 59). |
| 13 | Нет сохранения настроек | Low | — | Настройки только в `config/settings.py` | Не сделано (раздел 79). |
| 14 | Нет настоящего кросс-анализа пар | Medium | — | EUR/GBP считается через относительную силу, а не как самостоятельный рынок | Не сделано (раздел 38). |
| 15 | Нет центрального `DataAvailabilityGuard` | **High** | проверки разбросаны | Сейчас защита живёт в `similarity.py` и в maturity-логике `returns.py` отдельно. Пункт 2 этого отчёта — прямое следствие такой разбросанности: баг сидел в одной из копий. | Не сделано (раздел 6). |

---

## Что действительно проверено

| Пункт | Статус | Чем подтверждено |
|---|---|---|
| Набор тестов проходит | **VERIFIED** | 188/188, `python -m unittest discover -s tests`, из чистой распаковки |
| Баг с датой исправлен | **VERIFIED** | Регрессионный тест падает на старом коде, проходит на новом |
| Защита от look-ahead работает на любой дате | **VERIFIED** | Тест с датой за границей `datetime64[ns]` даёт `LookaheadError` |
| Счётчик тестов не захардкожен | **VERIFIED** | `scripts/count_tests.py` вернул 188, README обновлён скриптом |
| `validate_cftc.py` не подделывает результат | **VERIFIED** | Прогнан: вернул `BLOCKED`, код возврата 2 |
| **Сверка с живым CFTC** | **NOT VERIFIED — LIVE API BLOCKED** | Egress-прокси среды: `HTTP 403 Host not in allowlist`. Запускайте у себя. |
| `python -m pytest` проходит | **NOT VERIFIED** | pytest не установить: PyPI недоступен. Конфигурация написана, прогон за вами. |
| Интерфейс | **NOT VERIFIED** | JS исполнялся под Node с заглушкой DOM; в браузере не открывался |
| Индикатор TradingView | **NOT VERIFIED** | Pine исполняется только на серверах TradingView |

---

## Что делать дальше, по вашему же порядку

**P0 — осталось:** централизовать `DataAvailabilityGuard` (пункт 15). Это
не косметика: баг №2 существовал именно потому, что проверок было
несколько и разных.

**P1:** non-overlapping и block bootstrap, разделение COT ONLY / COT +
PRICE, отрицательные горизонты в event study, эпизоды событий.

**P2:** конструктор условий, Historical Explorer, сравнение дат.

**P3:** AI-слой по формату из раздела 49, PDF-отчёты.

**P4:** ML — только после доказательства, что он бьёт простую модель
out-of-sample.

---

## Честно о масштабе

99 разделов — это работа не на один проход. Я закрыл P0 и говорю об этом
прямо, вместо того чтобы объявить готовым то, что не проверял. Если
скажете, каким разделом заняться следующим, возьму его целиком, а не
поверхностно.
